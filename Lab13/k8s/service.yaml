apiVersion: v1
kind: Service
metadata:
  name: ml-api
  labels:
    app: ml-api
spec:
  type: NodePort
  selector:
    app: ml-api
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080
